version: '3.8'

services:
  postfix-relay:
    image: akhaladze/postfix-gmail:latest
    container_name: postfix-smtp-gmail
    restart: unless-stopped
    ports:
      - "25:25"
    env_file:
      - stack.env
